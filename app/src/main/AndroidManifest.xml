<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.NFC" />

    <uses-feature android:name="android.hardware.nfc" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".WarrantyActivity"
            android:label="@string/title_activity_warranty"
            android:theme="@style/AppTheme.NoActionBar.NoActionBar"></activity>
        <activity android:name="Activities.CreateDocsActivity"></activity>
        <activity android:name="Activities.SearchProductActivity" />
        <activity
            android:name="Activities.NFCDisplayActivity"
            android:label="NFC Data Display">
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
        <activity
            android:name="Activities.NFCBussinesActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="Activities.CreateBillActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="Activities.Register_Bussines_Activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="Activities.Register_Customer_Activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="Activities.ChooseUserActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="Activities.Business_HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="Activities.Customer_HomeActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="Activities.Login_Activity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>